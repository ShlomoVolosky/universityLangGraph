# LangSmith Trace Exports

This directory will contain LangSmith run links and screenshots from real query runs.

Minimum required (populated in Phase 10):
- One happy-path run with a 3-table join
- One retry scenario
- One rejected-non-SELECT scenario
